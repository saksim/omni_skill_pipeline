apiVersion: v1
kind: Service
metadata:
  name: omni-skill-pipeline
  labels:
    app.kubernetes.io/name: omni-skill-pipeline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: omni-skill-pipeline
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
